@model WebCrawler.Web.Mvc.ViewModels.CrawlerSessionViewModel

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>CrawlerSessionViewModel</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.Title)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Title)
            @Html.ValidationMessageFor(model => model.Title)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.StartUrl)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.StartUrl)
            @Html.ValidationMessageFor(model => model.StartUrl)
        </div>

        @Html.HiddenFor(model => model.Id)

      
        <div class="editor-label">
            @Html.LabelFor(model => model.SearchDepth)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.SearchDepth)
            @Html.ValidationMessageFor(model => model.SearchDepth)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
